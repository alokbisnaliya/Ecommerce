<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Product</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-zinc-300 flex items-center justify-center min-h-screen">

    <!-- Main Container -->
    <div class="w-full max-w-lg bg-zinc-800 p-10 rounded-lg shadow-lg">
        
        <!-- Page Title -->
        <h2 class="text-2xl font-semibold text-center mb-6 text-white">Update Product</h2>

        <!-- ✅ Corrected Form -->
        <form class="flex flex-col" action="/owners/products/update/<%= product._id %>" method="POST">
            
            <!-- Product Name -->
            <label class="text-sm font-medium mb-1">Product Name</label>
            <input type="text" name="name" 
                class="px-5 py-2 mb-3 bg-transparent rounded-md border-2 border-zinc-700 outline-none focus:border-blue-500"
                value="<%= product.name %>">

            <!-- Product Image URL -->
            <label class="text-sm font-medium mb-1">Image URL</label>
            <input type="text" name="image" 
                class="px-5 py-2 mb-3 bg-transparent rounded-md border-2 border-zinc-700 outline-none focus:border-blue-500"
                value="<%= product.image %>">

            <!-- Product Price -->
            <label class="text-sm font-medium mb-1">Price ($)</label>
            <input type="number" name="price" min="0" step="0.01"
                class="px-5 py-2 mb-3 bg-transparent rounded-md border-2 border-zinc-700 outline-none focus:border-blue-500"
                value="<%= product.price %>">

            <!-- Update Button -->
            <button type="submit"
                class="px-5 py-2 mt-4 bg-blue-600 text-white font-semibold rounded-md cursor-pointer hover:bg-blue-700 transition">
                Update Product
            </button>
        </form>

        <!-- Go Back Button -->
        <div class="mt-4 text-center">
            <a href="/owners/products" class="text-sm text-blue-400 hover:underline">← Back to Products</a>
        </div>

    </div>

</body>
</html>
